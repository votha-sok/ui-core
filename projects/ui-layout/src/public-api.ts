/*
 * Public API Surface of ui-layout
 * This is the ONLY file other projects may import from.
 * Everything exported here becomes part of the package's public contract.
 */

// TODO: replace with real exports as this library is built out in upcoming steps.
// This placeholder export exists only so the file is a valid ES module
// (required by isolatedModules / ng-packagr entry-point resolution).
export {};
